node_modules/
.env
*.log
# spec file holds a live token — never commit
deen.txt
# local spec notes — never commit
giveaways.txt
newone.txt
mee.txt
# live deploy secrets — never commit
render-secrets.txt
monetag-postback-key.txt
monetag-postback-link.txt
